List RabbitMQ Queues
--------------------
In order to use the management module you first need to install the python library, `requests`.
::

    pip install amqpstorm3[management]

.. literalinclude:: ../../examples/management/list_queues.py